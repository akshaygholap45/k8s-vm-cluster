---
- name: Deploy common packages
  hosts: all
  become: true
  roles:
    - commons
    - configure-kernel
    - containerd

# ====== Setup & Configure Master Node ==============
- name: Setup Master
  hosts: master
  become: true
  roles:
    - smbshare-setup
    - cluster-setup
  # ==== Copy the Cluster joining script from Master to localhost ===========
  tasks:
    - name: Copy from remote
      ansible.builtin.fetch:
        src: /srv/smbshare/join_cluster.sh
        dest: join_cluster.sh
        flat: true
      tags: copy-from-remote

# ==== Play to setup and configure k8s worker nodes ==========
- name: Join cluster
  hosts: worker
  become: true
  roles:
    - worker-setup
